<UserControl x:Class="ProfitFood.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ProfitFood.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="900"
             d:DesignWidth="1400"
             d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel, IsDesignTimeCreatable=True}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <SolidColorBrush x:Key="PageBackgroundBrush" Color="#FFF5F7FA" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="White" />
        <SolidColorBrush x:Key="BorderBrushLight" Color="#D9E2EC" />
        <SolidColorBrush x:Key="ForegroundPrimary" Color="#102A43" />
        <SolidColorBrush x:Key="ForegroundSecondary" Color="#486581" />
        <SolidColorBrush x:Key="SuccessBrush" Color="#137333" />
        <SolidColorBrush x:Key="WarningBrush" Color="#B26A00" />
        <SolidColorBrush x:Key="DangerBrush" Color="#B42318" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2F6FED" />

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushLight}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="16" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="Background" Value="#2F6FED" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#2F6FED" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.82" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#334E68" />
            <Setter Property="BorderBrush" Value="#BCCCDC" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D9E2EC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowBackground" Value="White" />
            <Setter Property="AlternatingRowBackground" Value="#F8FBFF" />
            <Setter Property="HorizontalGridLinesBrush" Value="#E6EDF5" />
            <Setter Property="VerticalGridLinesBrush" Value="#F0F4F8" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="SelectionUnit" Value="FullRow" />
            <Setter Property="RowHeight" Value="34" />
            <Setter Property="ColumnHeaderHeight" Value="38" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F0F4F8" />
            <Setter Property="Foreground" Value="#243B53" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderBrush" Value="#D9E2EC" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="10,8" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="#102A43" />
            <Setter Property="BorderBrush" Value="#EEF2F6" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DCEBFF" />
                    <Setter Property="Foreground" Value="#102A43" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EEF6FF" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Margin="16,16,16,8" Style="{StaticResource CardBorderStyle}">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="Главная панель" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource ForegroundPrimary}" />
                    <TextBlock Text="Оперативное состояние питания на сегодня" Margin="0,6,0,0" FontSize="13" Foreground="#627D98" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Создать меню" Width="120" Margin="0,0,8,0" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding CreateMenuCommand}" />
                    <Button Content="Открыть меню" Width="120" Margin="0,0,8,0" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding OpenTodayMenuCommand}" />
                    <Button Content="Печать формы 299" Width="150" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding PrintRequirementCommand}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- KPI -->
        <Grid Grid.Row="1" Margin="16,0,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Margin="0,0,8,0" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Дата" Foreground="{StaticResource ForegroundSecondary}" />
                    <TextBlock Text="{Binding TodayDateText}" Margin="0,8,0,0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource ForegroundPrimary}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Margin="0,0,8,0" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Детей на питании" Foreground="{StaticResource ForegroundSecondary}" />
                    <TextBlock Text="{Binding TotalChildren}" Margin="0,8,0,0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource ForegroundPrimary}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Margin="0,0,8,0" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Блюд в меню" Foreground="{StaticResource ForegroundSecondary}" />
                    <TextBlock Text="{Binding DishCount}" Margin="0,8,0,0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource ForegroundPrimary}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Статус меню" Foreground="{StaticResource ForegroundSecondary}" />
                    <TextBlock Text="{Binding MenuStatus}" Margin="0,8,0,0" FontSize="22" FontWeight="Bold" Foreground="{Binding MenuStatusBrush}" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main content -->
        <Grid Grid.Row="2" Margin="16,0,16,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="2.2*" />
            </Grid.ColumnDefinitions>

            <!-- Quick actions -->
            <Border Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Быстрые действия" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource ForegroundPrimary}" />

                    <Button Content="Создать меню на сегодня" Margin="0,16,0,8" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding CreateMenuCommand}" />
                    <Button Content="Открыть меню" Margin="0,0,0,8" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding OpenTodayMenuCommand}" />
                    <Button Content="Рассчитать продукты" Margin="0,0,0,8" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CalculateProductsCommand}" />
                    <Button Content="Создать меню-требование" Margin="0,0,0,8" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CreateRequirementCommand}" />
                    <Button Content="Печать формы 299" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding PrintRequirementCommand}" />
                </StackPanel>
            </Border>

            <!-- Menu today -->
            <Border Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" LastChildFill="False">
                        <TextBlock Text="Меню на сегодня" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource ForegroundPrimary}" DockPanel.Dock="Left" />
                        <Button Content="Открыть меню" DockPanel.Dock="Right" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding OpenTodayMenuCommand}" />
                    </DockPanel>

                    <DataGrid Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" ItemsSource="{Binding TodayMenuItems}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Прием пищи" Binding="{Binding MealTypeName}" Width="1.3*" IsReadOnly="True" />
                            <DataGridTextColumn Header="Блюдо" Binding="{Binding DishName}" Width="2.2*" IsReadOnly="True" />
                            <DataGridTextColumn Header="Техкарта" Binding="{Binding RecipeCardNumber}" Width="1*" IsReadOnly="True" />
                            <DataGridTextColumn Header="Выход" Binding="{Binding OutputText}" Width="0.9*" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Warnings -->
            <Border Grid.Row="1" Grid.Column="0" Margin="0,0,8,0" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Контроль" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource ForegroundPrimary}" />

                    <ItemsControl Grid.Row="1" Margin="0,16,0,0" ItemsSource="{Binding Warnings}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,8" Padding="10" CornerRadius="8" Background="#FFF4E5" BorderBrush="#F0B429" BorderThickness="1">
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="#8D2B0B" />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>

            <!-- Critical stock + last docs -->
            <Grid Grid.Row="1" Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*" />
                    <ColumnDefinition Width="1.3*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Margin="0,0,8,0" Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Критические остатки" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource ForegroundPrimary}" />

                        <DataGrid Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" ItemsSource="{Binding CriticalStockProducts}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Продукт" Binding="{Binding ProductName}" Width="1.8*" IsReadOnly="True" />
                                <DataGridTextColumn Header="Остаток" Binding="{Binding StockText}" Width="1*" IsReadOnly="True" />
                                <DataGridTextColumn Header="Мин." Binding="{Binding MinStockText}" Width="1*" IsReadOnly="True" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Последние документы" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource ForegroundPrimary}" />

                        <DataGrid Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" ItemsSource="{Binding LastDocuments}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Документ" Binding="{Binding DocumentName}" Width="2*" IsReadOnly="True" />
                                <DataGridTextColumn Header="Дата" Binding="{Binding DateText}" Width="1*" IsReadOnly="True" />
                                <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="1.2*" IsReadOnly="True" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- Status bar -->
        <StatusBar Grid.Row="3" Margin="16,0,16,16">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>