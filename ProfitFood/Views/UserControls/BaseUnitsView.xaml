<UserControl
    x:Class="ProfitFood.UI.Views.UserControls.BaseUnitsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:baseunitviewmodels="clr-namespace:ProfitFood.UI.ViewModels.BaseUnitViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProfitFood.UI.Views.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:ProfitFood.UI.ViewModels"
    d:DataContext="{d:DesignInstance Type=baseunitviewmodels:BaseUnitTabViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  Для панели кнопок  -->
            <RowDefinition Height="*" />
            <!--  Для таблицы  -->
        </Grid.RowDefinitions>

        <!--  Панель инструментов  -->
        <DockPanel
            Grid.Row="0"
            Margin="5"
            LastChildFill="True">
            <Button
                Width="80"
                Margin="0,0,5,0"
                Command="{Binding AddProductCommand}"
                Content="Добавить" />
            <Button
                Width="80"
                Margin="0,0,5,0"
                Command="{Binding EditProductCommand}"
                Content="Редактировать" />
            <Button
                Width="80"
                Command="{Binding DeleteProductCommand}"
                Content="Удалить" />
            <TextBox Margin="5,0,5,0" Text="{Binding SearchBaseUnit}" />
        </DockPanel>

        <!--  Табличная часть  -->
        <DataGrid
            Grid.Row="1"
            Margin="5"
            AutoGenerateColumns="False"
            BorderBrush="Gray"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{Binding BaseUnits}"
            SelectedItem="{Binding SelectedBaseUnit}">

            <DataGrid.Resources>
                <Style TargetType="{x:Type DataGridRow}">
                    <Setter Property="BorderBrush" Value="LightGray" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Name}"
                    Header="Наименование" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>